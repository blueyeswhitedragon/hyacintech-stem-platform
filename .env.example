# Hyacintech STEM Platform
# Copy to .env, replace every placeholder, and never commit the resulting .env file.

# --- Required server settings ---

# SQLite URL. Relative file paths are resolved from prisma/ by Prisma.
DATABASE_URL="file:./dev.db"

# iron-session secret: at least 32 characters. Generate with: openssl rand -base64 32
SESSION_SECRET="please-change-me-to-a-random-32+char-secret"

# Configure at least one valid provider key. Do not leave a placeholder in production.
# If both keys are real, LLM_PROVIDER is required so the server cannot silently choose one.
OPENAI_API_KEY=sk-your-openai-api-key-here
# OPENAI_API_BASE=https://api.openai.com/v1
# Competition Qwen3.5-35B gateways may expose a real base URL ending in /v1/v1; preserve the vendor-provided path exactly.
# OPENAI_API_BASE=https://competition-gateway.example/v1/v1
# DEEPSEEK_API_KEY=sk-your-deepseek-api-key-here
# DEEPSEEK_API_BASE=https://api.deepseek.com

# --- Data Lab administrator ---
# Required by `npm run data-lab:init`; `npm run db:seed` only uses them when all three are set.
# ADMIN_USERNAME="data-admin"
# ADMIN_PASSWORD="change-me-strong-password"
# ADMIN_DISPLAY_NAME="数据平台主管"

# Master secret for Data Lab credentials stored with ENCRYPTED_DB. It is not
# required when every Data Lab connection references an environment variable.
# Generate independently from SESSION_SECRET with: openssl rand -base64 32
# Rotating it requires a credential re-encryption procedure.
# DATA_LAB_CREDENTIAL_MASTER_KEY="please-change-me-to-an-independent-random-secret"

# --- Runtime LLM overrides (optional) ---
# Provider is auto-detected only when exactly one valid key is configured.
# LLM_PROVIDER=openai
# LLM_MODEL=gpt-4o
# Use a stable identity when a gateway's model name can point to changing weights.
# LLM_MODEL_TAG=openai:gpt-4o
# DeepSeek V4 keeps thinking enabled. Token values are output ceilings.
# LLM_THINKING=enabled
# LLM_REASONING_EFFORT=high
# LLM_MAX_TOKENS=16000
# LLM_TIMEOUT_MS=180000
# TUTOR_LLM_MAX_TOKENS=16000
# TUTOR_LLM_TIMEOUT_MS=180000
# STUDENT_LLM_MAX_TOKENS=10000
# STUDENT_LLM_TIMEOUT_MS=180000
# EVALUATOR_LLM_MAX_TOKENS=20000
# EVALUATOR_LLM_TIMEOUT_MS=300000
# Fact extraction does not need long reasoning; disabling thinking prevents reasoning tokens from consuming the JSON budget.
# EVALUATOR_LLM_THINKING=disabled

# Structured state extraction can use a smaller/dedicated model.
# EXTRACTOR_LLM_PROVIDER=deepseek
# EXTRACTOR_LLM_MODEL=deepseek-v4-pro

# --- Data Lab generation and governance (optional) ---
# Legacy emergency defaults. New Data Lab batches should select database-backed RuntimeBundles in the UI.
# Keep A/B on different model families. These values remain only for scripts and migration fallback.
# DATA_LAB_MODEL_A_PROVIDER=openai
# DATA_LAB_MODEL_A=Qwen3.5-35B-A3B
# DATA_LAB_MODEL_B_PROVIDER=deepseek
# DATA_LAB_MODEL_B=deepseek-v4-pro

# AI-assisted TopicCard curation and first-review drafts.
# DATA_LAB_AI_CURATOR_PROVIDER=deepseek
# DATA_LAB_AI_CURATOR_MODEL=deepseek-v4-pro

# Emergency switch: false blocks deployment mutations without changing stored state.
# ENABLE_MODEL_DEPLOYMENT=true
# Promote the current runtime prompt as the initial registered baseline when bootstrapping.
# PROMOTE_RUNTIME_PROMPT_BASELINE=false

# --- Development only (optional) ---
# Additional dev hostnames, comma-separated, without protocol or path.
# 127.0.0.1 is allowed by default; list LAN, reverse-proxy, or HTTPS tunnel hostnames here.
# Required whenever `next dev` is reached through any host other than localhost:
# unlisted hosts get 403 on /_next assets, so pages load but never hydrate and
# the login form degrades into a native GET submit (URL becomes /auth/login?).
# NEXT_ALLOWED_DEV_ORIGINS=hyacintech.cn,www.hyacintech.cn,192.168.1.20

# --- Script/test only (do not set on the production service) ---
# DATA_LAB_TEST_CAMPAIGN=dataset-base-v1-pilot-12
# DATA_LAB_USE_TEST_RELEASE_DIR=true
# TEST_BASE_URL=http://localhost:3000
# MODEL_TAG=candidate-model-tag
# JUDGE_API_KEY=sk-judge-key
# JUDGE_API_BASE=https://example.com/v1
# JUDGE_MODEL=judge-model-name
# JUDGE_TIMEOUT_MS=180000
# JUDGE_MAX_TOKENS=2000
# ALLOW_LEGACY_ONE_SHOT_DISTILL=false
# SMARTEDU_SEARCH_URL=https://x-search.ykt.eduyun.cn/v1/resources/combine/search
# SMARTEDU_APP_ID=e5649925-441d-4a53-b525-51a2f1c4e0a8
# SMARTEDU_CHANNEL_ID=3303d351-a43f-4580-9a43-58a929595fda
# SMARTEDU_SECTION_HASH=229c170fd24d459901894ffdd2a5c350f1246827be6f541ba6caf641133168cc
# SMARTEDU_CHANNEL_BASE=https://your-smartedu-host.example/api/channel/section
# NGROK_API_BASE=http://127.0.0.1:4040
