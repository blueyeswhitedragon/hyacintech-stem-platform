import TrainingRunForm from '@/app/components/dataLab/TrainingRunForm';
import { listReleases, listTrainingRuns } from '@/app/lib/dataLab/service';

export default async function TrainingRunsPage(){const [releases,runs]=await Promise.all([listReleases(),listTrainingRuns()]);return <div className="space-y-6"><div><h1 className="text-2xl font-semibold">外部训练任务</h1><p className="mt-1 text-sm text-gray-500">记录主办方平台任务与参数；本系统不保存平台密钥，也不自动提交训练。</p></div><TrainingRunForm releases={releases.filter((release)=>release.status==='FROZEN').map((release)=>({id:release.id,version:release.version}))}/><div className="overflow-x-auto border bg-white"><table className="w-full text-left text-sm"><thead className="border-b bg-gray-50"><tr><th className="p-3">任务</th><th className="p-3">数据版本</th><th className="p-3">基础模型</th><th className="p-3">外部ID</th><th className="p-3">状态</th><th className="p-3">模型标签</th></tr></thead><tbody>{runs.map((run)=><tr key={run.id} className="border-b last:border-0"><td className="p-3 font-medium">{run.name}</td><td className="p-3">{run.release.version}</td><td className="p-3">{run.baseModel}</td><td className="p-3 text-gray-600">{run.externalTaskId||'-'}</td><td className="p-3">{run.status}</td><td className="p-3">{run.modelTag||'-'}</td></tr>)}</tbody></table></div></div>}
