"use client";

import { useState } from 'react';
import { useRouter } from 'next/navigation';

export default function UserManager(){const router=useRouter();const [message,setMessage]=useState<string|null>(null);const [pending,setPending]=useState(false);async function create(formData:FormData){setPending(true);setMessage(null);try{const response=await fetch('/api/data-lab/users',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(formData))});const data=await response.json();if(!response.ok)throw new Error(data.error??'创建失败');router.refresh();setMessage('后台账号已创建');}catch(error){setMessage(error instanceof Error?error.message:String(error));}finally{setPending(false)}}return <form action={create} className="grid gap-3 border bg-white p-4 md:grid-cols-4"><label className="text-sm">用户名<input name="username" required className="mt-1 w-full border px-3 py-2" /></label><label className="text-sm">显示名称<input name="displayName" required className="mt-1 w-full border px-3 py-2" /></label><label className="text-sm">密码<input name="password" type="password" minLength={8} required className="mt-1 w-full border px-3 py-2" /></label><label className="text-sm">角色<select name="role" className="mt-1 w-full border px-3 py-2"><option value="annotator">标注者</option><option value="reviewer">复审者</option><option value="admin">管理员</option></select></label><div className="flex items-center gap-3 md:col-span-4"><button disabled={pending} className="bg-gray-950 px-4 py-2 text-sm text-white">创建账号</button>{message&&<span className="text-sm text-gray-600">{message}</span>}</div></form>}
